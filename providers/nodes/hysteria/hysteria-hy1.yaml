# ===========================================
# Hysteria v1 基础配置 (旧协议)
# 参考: https://wiki.metacubex.one/en/config/proxies/hysteria
# ===========================================

- name: "hy1-basic"
  type: hysteria
  server: server.example.com
  port: 443
  auth_str: "your-auth-string"     # 认证字符串
  # ip-version: dual                            # IP版本: dual/ipv4/ipv6/ipv4-prefer/ipv6-prefer
  # interface-name: ""                          # 网络接口 (如: eth0, tailscale0)
  # routing-mark: 0                             # 路由标记
  udp: true
  # obfs: "your-obfs-password"     # 混淆密码
  # protocol: udp                   # 协议 (默认 udp)
  # up: "50 Mbps"
  # down: "200 Mbps"
  # fast-open: true                 # 启用 TCP Fast Open
